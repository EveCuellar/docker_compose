services:
  frontend:
    build:
      context: ./Angular
    ports:
      - "4200:80"
    networks:
      - app-network
    depends_on:
      - backend

  backend:
    build:
      context: ./Api-Express
    ports:
      - "3000:3000"
    networks:
      - app-network
    environment:
      - MONGO_URI=mongodb://mongo:27017/animales
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    ports:
      - "27018:27017"
    networks:
      - app-network
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:

networks:
  app-network:
